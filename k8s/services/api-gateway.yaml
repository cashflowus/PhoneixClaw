apiVersion: apps/v1
kind: Deployment
metadata:
  name: api-gateway
  namespace: copytrader
  labels:
    app: api-gateway
spec:
  replicas: 2
  selector:
    matchLabels:
      app: api-gateway
  template:
    metadata:
      labels:
        app: api-gateway
    spec:
      containers:
        - name: api-gateway
          image: copytrader/api-gateway:latest
          ports:
            - containerPort: 8011
          envFrom:
            - configMapRef:
                name: copytrader-config
            - secretRef:
                name: copytrader-secrets
          readinessProbe:
            httpGet:
              path: /health
              port: 8011
            initialDelaySeconds: 10
            periodSeconds: 5
          livenessProbe:
            httpGet:
              path: /health
              port: 8011
            initialDelaySeconds: 15
            periodSeconds: 10
          resources:
            requests:
              cpu: "100m"
              memory: "256Mi"
            limits:
              cpu: "500m"
              memory: "512Mi"
---
apiVersion: v1
kind: Service
metadata:
  name: api-gateway
  namespace: copytrader
spec:
  selector:
    app: api-gateway
  ports:
    - port: 8011
      targetPort: 8011
