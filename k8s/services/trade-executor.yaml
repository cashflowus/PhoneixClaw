apiVersion: apps/v1
kind: Deployment
metadata:
  name: trade-executor
  namespace: copytrader
  labels:
    app: trade-executor
spec:
  replicas: 1
  selector:
    matchLabels:
      app: trade-executor
  template:
    metadata:
      labels:
        app: trade-executor
    spec:
      containers:
        - name: trade-executor
          image: copytrader/trade-executor:latest
          ports:
            - containerPort: 8008
          envFrom:
            - configMapRef:
                name: copytrader-config
            - secretRef:
                name: copytrader-secrets
          readinessProbe:
            httpGet:
              path: /health
              port: 8008
            initialDelaySeconds: 10
            periodSeconds: 5
          livenessProbe:
            httpGet:
              path: /health
              port: 8008
            initialDelaySeconds: 15
            periodSeconds: 10
          resources:
            requests:
              cpu: "100m"
              memory: "256Mi"
            limits:
              cpu: "500m"
              memory: "512Mi"
---
apiVersion: v1
kind: Service
metadata:
  name: trade-executor
  namespace: copytrader
spec:
  selector:
    app: trade-executor
  ports:
    - port: 8008
      targetPort: 8008
