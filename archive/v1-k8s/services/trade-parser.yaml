apiVersion: apps/v1
kind: Deployment
metadata:
  name: trade-parser
  namespace: phoenixtrader
  labels:
    app: trade-parser
spec:
  replicas: 2
  selector:
    matchLabels:
      app: trade-parser
  template:
    metadata:
      labels:
        app: trade-parser
    spec:
      containers:
        - name: trade-parser
          image: phoenixtrader/trade-parser:latest
          ports:
            - containerPort: 8006
          envFrom:
            - configMapRef:
                name: phoenixtrader-config
            - secretRef:
                name: phoenixtrader-secrets
          readinessProbe:
            httpGet:
              path: /health
              port: 8006
            initialDelaySeconds: 10
            periodSeconds: 5
          livenessProbe:
            httpGet:
              path: /health
              port: 8006
            initialDelaySeconds: 15
            periodSeconds: 10
          resources:
            requests:
              cpu: "100m"
              memory: "256Mi"
            limits:
              cpu: "500m"
              memory: "512Mi"
---
apiVersion: v1
kind: Service
metadata:
  name: trade-parser
  namespace: phoenixtrader
spec:
  selector:
    app: trade-parser
  ports:
    - port: 8006
      targetPort: 8006
